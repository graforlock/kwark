var select = require('../src/kwark-dom-select'),
    effects = require('../src/kwar-dom-select'),
    test = require('tape');

test('select and nodeify', function(t) { 
    t.plan(1);
    t.end();
});

test('fade out after three seconds', function(t) { 
    t.plan(1);
    t.end();
});


